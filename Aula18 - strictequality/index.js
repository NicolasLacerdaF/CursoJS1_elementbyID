// = assignment operator(operador de atribuição)
// == comparisor operator (campara se valores são iguais)
// === strict equality operator (campara se os valores e o tipo de dados são iguais, incluindo strings e tudo mais)
// != inequality operator 
// !== strict inequality operator


const PI = "3.14";

if(PI !== "3.14"){
    console.log("That is not Pi");
}
else{
    console.log("That is Pi");
}